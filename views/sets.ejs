<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lego Collection</title>
    <link rel="stylesheet" href="/css/main.css" />
</head>

<body tabindex="-1" data-new-gr-c-s-check-loaded="14.1134.0" data-gr-ext-installed="">

    <%- include('partials/navbar', {page: '/lego/sets' }) %>

    <div class="container mx-auto p-4">
        <div class="hero bg-base-200 rounded-lg p-6">
            <div class="hero-content text-center ">
                <div class="max-w-md">
                    <h1 class="text-5xl font-bold">Collection</h1>
                    <p class="py-6">Browse the entire collection. Click the theme to filter by themes such as: <a
                            href="/lego/sets?theme=Technic" class="btn btn-primary btn-xs">Technic</a> <a
                            href="/lego/sets?theme=Classic Town" class="btn btn-primary btn-xs">Classic Town</a> <a
                            href="/lego/sets?theme=Supplemental" class="btn btn-primary btn-xs">Supplemental</a>
                    </p>
                </div>
            </div>
        </div>
        <br>

        <!-- Table to display Lego sets -->
        <div class="overflow-x-auto overflow-y-hidden">
            <table class="table table-zebra">
                <!-- <thead>
                <tr>
                <th>Image</th>
                <th>Name</th>
                <th>Theme</th>
                <th>Year</th>
                <th>Num Parts</th>
                <th>Details</th>
                </tr>
                </thead> -->
                <tbody>
                <!-- Iterate over sets array and generate table rows -->
                    <% sets.forEach(set=> { %>
                        <tr>
                            <!-- Image -->
                            <td><img src="<%= set.img_url %>" alt="<%= set.name %>"class="avatar w-32 h-32 rounded-lg bg-transparent bg-no-repeat bg-cover"></td>
                            <!-- Name --><!-- Theme -->
                            <td><%= set.name %> | <a href="/lego/sets?theme=<%= set.Theme.name %>" class="btn mt-2 btn-xs"><%= set.Theme.name %></a></td>
                            <!-- Year --><!-- Num Parts -->
                            <td><span class="font-bold">Year: </span><%= set.year %> - <span class="font-bold">Parts:</span><%= set.num_parts %></td>
                            <!-- Details Link -->
                            <td><a href="/lego/sets/<%= set.set_num %>" class="btn btn-xs">Details</a></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</body>

</html>